<%= form_for(@reject) do |f| %>
  <% if @reject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reject.errors.count, "error") %> prohibited this rejected auction from being saved:</h2>

      <ul>
      <% @reject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table>
  <tr>
	<td><%= f.label 'Ad text'%></td>
	<td><%= f.text_area :adtext %></td>
  </tr>
  
</table>  


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
